# 2026-08-09 工作日志

## 23:45 台风板块高频刷新（自动化 automation-1786286346465 首次执行）
- 多源核实第13号台风「白海豚」最新状态，更新 `static/typhoon.json`，同步 `static/changelog.json` + `data/changelog.json`，commit `f54b956` 已推送 main。
- 关键变化：22时减弱为强热带风暴（11级，温州文成县境内，距射阳约654km）；盐城暴雨预警升级为黄色；射阳台风蓝色预警继续发布；按中国气象局专家解读订正后期路径（鄂豫皖交界回旋 → 12日后残涡转偏北）。
- 台风未停编，活跃期监测继续。

### 沉淀的项目约束（台风板块）
- 台风走向图投影边界：经度 115.5–128.0、纬度 24.5–36.2，track 坐标越界会画出画布。
- track 中 `current:true` 必须唯一（驱动台风眼动画位置）；预报点必须 `forecast:true`。
- `windRadius` 各半径为 falsy 判断，置 null 即隐藏该风圈。
- `public/` 已 gitignore，只需改 `static/` 源文件。
