# 2026-08-15 工作日志

## 每日自动更新（automation-1783388608608，07:50 触发）

完成 12 板块巡检，9 板块有新动态，JSON 全部更新并通过 `json.loads` 校验，git commit `2dcf188` 已推送触发部署。

**有更新板块**：
- 特斯拉：传最快本月发布冷气推进版 Roadster（待官方）
- 中药材：康美周报日指数 1489.3 点跌 0.18%、涨跌比 1:10
- 文玩手串：2026 按串价目（入门 300-1000 元、奇楠 12-18 万）
- 紫砂：Poly-Online 线上专场 70 把作品
- 游戏主机：Switch 2 因内存芯片短缺 9/1 涨价 50 美元
- 漫威：《复联5：毁灭之日》D23 开票+新海报（12-18 定档，与站点时间轴自洽）
- 漫威票房：北美 704.5M / 海外 11.08 亿（索尼之最）/ 中国 13.68 亿；订正破7亿天数「16→13天」
- FSD：监管重构下数据合规成瓶颈、北京城中村路测、Robotaxi 奥斯汀无监督
- 养生茶：卫健委推进地黄/天冬/麦冬/化橘红纳入食药物质目录
- 射阳：「小开海」梭子蟹丰收上市（114 艘渔船、25-65 元/斤）
- ChinaJoy：《影之刃零》预售登顶 Steam 第一、甄子丹监制、10-29 发售

**无更新**：台风（浪卡 2617 远洋减弱为热带低压、对我国无影响，无新系统影响射阳，未改 JSON）。

## 关键修正
- marvel-boxoffice 里程碑「北美破7亿天数」由 16 天订正为 13 天（北美 7-31 上映、8-13 破 7 亿，THR/CBC/Variety 一致）。

## 旅行页射阳行程卡片结构修复
- 用户反馈射阳板块"点不开"。
- 根因：射阳 `.tl-trip` article 被错误地嵌套在呼伦贝尔 `.tl-trip` article 内部。当呼伦贝尔行程折叠时，其 `.tl-trip-body` 为 `display:none`，导致射阳的 body 即使被点击展开也看不到。
- 修复：移除呼伦贝尔 trip 关闭标签后多余的关闭序列，使射阳 trip 与呼伦贝尔 trip 成为 `.tl-list` 下的同级元素。
- 验证：本地 `hugo --gc` 构建通过；`public/travel.html` 解析确认 6 个 trip 的父级均为 `.tl-list`；`node scripts/encrypt_travel.mjs --selftest` 通过；commit `39fc0be` 已推送，GitHub Actions 部署成功（run 31855931585）。

## 旅行页射阳个人合影移入行程体内
- 用户反馈射阳行程"能点开了，但是没照片"。
- 根因：两张宋晓峰合影之前被收到页面顶部独立的 `#personalPhotos` `<details>` 折叠块里，射阳行程展开后只有文字说明。
- 修复：删除顶部 `#personalPhotos` section，把两张合影及 caption 直接放入射阳 `.tl-visit-body` 的 `.tl-gallery-block`；点击展开射阳行程后即可直接看到照片。
- 清理：移除不再使用的 `.tl-details` / `.tl-details-sum` 样式。
- 验证：本地 `hugo --gc` 构建通过；`public/travel.html` 确认射阳 visit-body 内含 2 张 `song-xiaofeng` 照片；`node scripts/encrypt_travel.mjs --selftest` 通过；commit `7ef77ef` 已推送，GitHub Actions 部署成功（run 31856439938）。

## 导航栏「更多」下拉桌面端打不开（严重 bug 修复）
- 用户反馈：电脑端主页上方「更多」点击无反应。
- 根因：`nav-more-btn` 同时挂了行内 `onclick`（`this.parentElement.classList.toggle('open')`）与 `app.js` 区域H 的 click 监听器（`wrap.classList.toggle('open')`），两者各 toggle 一次互相抵消 → 永远打不开。CSP 含 `unsafe-inline`，行内 handler 未被拦，双触发必现。
- 修复：`layouts/partials/navbar.html` 与 `scripts/sync_navbar.py` 模板删除行内 onclick；重跑 `sync_navbar.py` 刷新全站 170 个静态页；Hugo 构建刷 `public/`。全站仅保留 `app.js` 单一处理器（点击切换 + 点击别处关闭 + ESC 关闭）。
- 验证：全仓(含 public)行内 onclick 残留 0；线上首页确认按钮无 onclick 且 `app.js` 已加载；commit `94b6beb` 已推送，GitHub Actions 部署成功（run 31857826827）。
