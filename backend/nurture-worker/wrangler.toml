name = "longxiong-nurture"
main = "worker.js"
compatibility_date = "2024-09-23"

# KV 命名空间绑定：首次部署前需先创建（见 README），把得到的 id 填到下面。
# 创建命令：wrangler kv namespace create NURTURE
kv_namespaces = [
  { binding = "NURTURE", id = "REPLACE_WITH_KV_ID", preview_id = "REPLACE_WITH_KV_ID" }
]
