{
  "name": "gitalk-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: 替换已下线的 Gitalk 默认 CORS proxy (cors-anywhere.azm.workers.dev)",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
